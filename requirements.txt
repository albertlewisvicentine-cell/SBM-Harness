# SBM-Harness Python dependencies
# Pinned versions for hermetic builds and reproducibility

# Core dependencies
PyYAML==6.0.1

# Testing framework
pytest==7.4.3
pytest-cov==4.1.0
pytest-snapshot==0.9.0

# Code quality
coverage==7.3.2

# Schema validation
jsonschema==4.20.0

# For audit report generation (if needed in future)
# markdown==3.5.1
# weasyprint==60.1  # Uncomment if PDF generation is needed
